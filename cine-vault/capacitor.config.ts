import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pamelalotriet.cinevault',
  appName: 'cine-vault',
  webDir: 'dist/cine-vault/browser',
};

export default config;
